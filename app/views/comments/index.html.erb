<h1>Listing comments</h1>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Body</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.location_id %></td>
        <td><%= comment.name %></td>
        <td><%= comment.body %></td>
        <td><%= link_to 'Show', comment %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

